/**
 * Copy to config.secrets.js (gitignored) on each environment.
 * Restrict the Places API key to your domain in Google Cloud Console.
 */
window.RIVERBIRD_SECRETS = {
  placesApiKey: 'YOUR_GOOGLE_PLACES_API_KEY'
};
